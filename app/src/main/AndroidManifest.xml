<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.IndexActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.StoryActivity"
            android:parentActivityName=".ui.IndexActivity" />
        <activity android:name=".ui.SecondStoryActivity"
            android:parentActivityName=".ui.IndexActivity"/>
        <activity android:name=".ui.ThirdStoryActivity"
            android:parentActivityName=".ui.IndexActivity"/>
        <activity android:name=".ui.QuizActivity" />
        <activity android:name=".ui.StartingScreenActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>